#include "main.h"

/**
 * swap_int - swaps the values of two integers
 * @a: the variable
 * @b: the variable
 * return: nothing
 *
 */

void swap_int(int *a, int *b)
{
	int swap;
	*a = swap;
	*b = *a;
	swap = *b;
}
