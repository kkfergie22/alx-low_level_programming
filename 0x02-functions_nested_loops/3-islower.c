#include "main.h"
/**
 *  _islower - function to check case of character
 *
 *  c - is the variable evaluated
 *
 *  return: 0
 */

int _islower(int c)
{
	if (c == 'a' && c == 'z')
	{
		return (1);
	}
	else
		return (0);
}
