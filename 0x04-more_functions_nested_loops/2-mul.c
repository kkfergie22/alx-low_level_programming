#include "main.h"
/**
 * mul - function which multiplies two integers
 * @a: first variable
 * @b: second variable
 * Return: result
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
